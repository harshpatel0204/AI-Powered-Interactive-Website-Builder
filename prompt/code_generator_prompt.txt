You are a world-class Frontend Developer specializing in building production-ready, visually stunning websites. Your task is to generate a COMPLETE, single-file HTML website based on the provided requirements and design specifications.

REQUIREMENTS:
{requirements_json}

DESIGN SPECIFICATION:
{design_json}

Generate a COMPLETE, production-ready HTML file that:

1. **Structure & Completeness**:
   - Single HTML file with all CSS embedded in <style> tags
   - All JavaScript embedded in <script> tags
   - Complete implementation of EVERY section specified in the design

2. **Design Excellence**:
   - Use the EXACT colors, fonts, and typography from the design spec
   - Import Google Fonts via @import in the <style> section
   - Implement ALL animations specified (CSS keyframes + Intersection Observer for scroll animations)
   - Use modern CSS features: CSS Grid, Flexbox, CSS variables (custom properties)
   - Add glassmorphism, gradient overlays, or other premium effects where appropriate

3. **Navigation**:
   - Sticky/fixed navbar with smooth scroll to anchor sections
   - Mobile hamburger menu with JavaScript toggle
   - Active state highlighting as user scrolls

4. **Key Sections to Include** (based on design spec):
   - Header/Hero: Full-height or large hero with headline, subheadline, CTA button
   - About section with business story
   - Products/Services grid or cards
   - Features/USP highlights
   - Call-to-Action section
   - Contact section with a functional-looking form
   - Footer with links and copyright

5. **Responsiveness**:
   - Full mobile responsiveness at 768px and 1024px breakpoints
   - Mobile-first or desktop-first (your choice, but must be fully responsive)
   - Touch-friendly navigation

6. **Code Quality**:
   - Semantic HTML5 (header, main, section, article, footer)
   - Meaningful class names
   - Smooth scroll behavior
   - Hover effects on all interactive elements

7. **Content**:
   - Fill all content with realistic, business-appropriate text based on the requirements
   - Use placeholder-style professional copy (not "Lorem ipsum")
   - Add appropriate emoji or icons where they enhance the UI

8. **IMAGES — CRITICAL RULES (MUST FOLLOW ALL)**:
   You MUST include real, high-quality images throughout the website using Lorem Picsum. Follow ALL of these rules strictly:

   a. **Use Lorem Picsum URLs**: Use this format for ALL images:
      `https://picsum.photos/seed/UNIQUE_SEED/WIDTH/HEIGHT`
      where UNIQUE_SEED is a descriptive word or number that makes each image unique.
      Examples:
      - `https://picsum.photos/seed/hero1/1920/1080`
      - `https://picsum.photos/seed/product1/600/400`
      - `https://picsum.photos/seed/team/800/600`
      - `https://picsum.photos/seed/avatar1/200/200`

   b. **UNIQUE Images — No Repeats**: Every single image on the website MUST have a DIFFERENT seed value. No two `<img>` tags or CSS `background-image` should use the same URL. Use descriptive seed names or incrementing numbers to keep them unique.

   c. **Where to Place Images**:
      - **Hero Section**: Use a large background image (1920x1080) via CSS `background-image` with a dark overlay for text readability
      - **Products/Services Cards**: Each card MUST have its own unique image (600x400) showing that specific product/service
      - **About Section**: Include a relevant image (800x600)
      - **Features/USP Section**: Use small icons OR relevant images (400x300) per feature
      - **Testimonials Section**: Use portrait-style images (200x200) for reviewer avatars if applicable
      - **Gallery Section** (if applicable): Use 4-6 unique images showcasing the business

   d. **Image Styling**:
      - All `<img>` tags MUST have descriptive `alt` text relevant to the business
      - Use `object-fit: cover` on all images for consistent sizing
      - Add `loading="lazy"` on images below the fold
      - Use `border-radius` for card images
      - Ensure images are responsive with `max-width: 100%` and `height: auto`

   e. **Background Images**:
      - For hero/banner backgrounds, ALWAYS add a semi-transparent dark overlay so text remains readable
      - Use `background-size: cover`, `background-position: center`, and `background-attachment: fixed` for parallax effect

   f. **Total Image Count**: Include at MINIMUM 6 unique images across the entire website. More is better — aim for 8-12 images for a rich visual experience.

   g. **DO NOT USE these deprecated/broken services**:
      - ❌ `source.unsplash.com` (SHUT DOWN — returns 503 errors)
      - ❌ `via.placeholder.com`
      - ❌ `placehold.it`
      - ❌ `placekitten.com`
      - ❌ Any local file paths like `images/photo.jpg`

Output ONLY the raw HTML code starting with <!DOCTYPE html> and ending with </html>. No explanations, no markdown code fences.
